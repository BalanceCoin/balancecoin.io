version: "3"

services:
  watchtower:
    image: v2tec/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - REPO_USER=mattxlee
      - REPO_PASS=3Cef774!
    command: --interval 30 www

  frontend:
    container_name: www
    image: registry.cn-hongkong.aliyuncs.com/blc/balancecoin.io:release
    ports:
      - "0.0.0.0:80:80"
      - "0.0.0.0:443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
